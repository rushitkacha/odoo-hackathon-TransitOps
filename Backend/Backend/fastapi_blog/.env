APP_NAME=TransitOps
APP_VERSION=1.1.0
DEBUG=True

HOST=127.0.0.1
PORT=8000

# SQLite by default so the backend runs immediately with no external DB server.
# To use PostgreSQL instead, comment the line below and uncomment the psycopg2 line.
DATABASE_URL=sqlite:///./transitops.db
# DATABASE_URL=postgresql+psycopg2://postgres:your_password@localhost:5432/transitops_db

SECRET_KEY=s3_csYeXTXu-FMVkZk5LDFLNfCXP6YALCxE1-1QIsspHpGRxD7stOM6sxRXI5CcEAslUMAm543C1ib9QcShZww
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# Comma-separated origins allowed by CORS (the static frontend server).
CORS_ORIGINS=http://localhost:5500,http://127.0.0.1:5500,http://localhost:5501,http://127.0.0.1:5501
